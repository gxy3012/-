线性表学习笔记三：栈

栈的顺序表示：

初始化：
int a[100];
int top=-1;

入栈：
int push(int* a,int top,int elem){
    a[++top]=elem;
    return top;
}

出栈：
int pop(int* a,int top）{
    if(top==-1){
    printf("空栈！")；
    return -1；
    }
    printf("栈顶元素：%d\n",a[top--]);
    return top;
}

主函数：
int main（）{
    int a[100];
    int top=-1;
    top=push(a,top,1);
    top=push(a,top,2）；
    top=push(a,top,3）；
    top=push(a,top,4）；
    top=push(a,top,5）；
}
